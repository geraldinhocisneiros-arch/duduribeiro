# Connection pooling (usada pela aplicação em tempo de execução)
DATABASE_URL="postgresql://user:password@host:6543/database?pgbouncer=true"
# Conexão direta, sem pooler (usada só para rodar as migrações do banco)
DIRECT_URL="postgresql://user:password@host:5432/database"
